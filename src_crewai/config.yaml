llm:
  provider: "google"
  model_name: "gemini-2.5-flash" # Excellent for speed and quota-friendly
  temperature: 0.1 # Low temperature for analytical, factual reasoning
  
embedder:
  provider: "google-generativeai"
  model_name: "models/embedding-001" # Required for CrewAI's memory system